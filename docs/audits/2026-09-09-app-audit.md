# Auditoría 2026-09-09 de la app (visual · código · funcionalidad · lógica de entrenamiento) y plan de remediación v11.70 → v11.74

> Estado: **en ejecución desde 2026-09-10** — v11.70 (P0 + datos + seguridad) primero; después v11.71 (motores y pack),
> v11.72 (UX), fn v5 (servidor) y v11.73 (código y tests). Informe de auditoría **y** plan de arreglo, como el del 08-sep
> (`2026-09-08-app-audit.md`, cuya remediación v11.66-v11.68 es la línea base de éste).

## Context

**Qué se pidió.** Julian (2026-09-09, tras v11.69): "Haz una auditoría visual, de código, de funcionalidades completa
para verificar si hay mejoras para realizar". Llega 24 h después de la remediación del 08-sep (v11.66-v11.68) y el
mismo día de v11.69 (Withings completo, voz del coach, modo manual). El sistema ya tiene WHOOP y Withings vivos, la
primera revisión del coach en Home, 0 comidas registradas (empieza hoy) y la próxima revisión el domingo 13.

**Método.** Tres revisiones de sólo lectura en paralelo (visual/UX · código/robustez/tests · funcionalidad/lógica/
research), los advisors de seguridad y rendimiento de Supabase, los logs de las funciones (24 h), `cron.job_run_details`
(3 días) y `integration_status`, y verificación manual en código de los 12 hallazgos más graves (12 confirmados). La
auditoría del 08-sep es la línea base: **lo que cerró no se repite** (B-1…B-6, E-1…E-18, R-1…R-11, V-1…V-9, I-1
verificados en pie). Todo hallazgo lleva `file:line`.

**Lo que está bien y no se toca:** tokens/eyebrow/padding unificados; `.todays-plan-*` y el resto del CSS muerto de
V-3 borrado; render pass en Home y Stats; `safeCall`; 36 tests en CI con los dos generadores en `--check`; el triple
de versión guardado por test; advisors limpios (sólo la protección de contraseñas, que es de Julian, y 3 INFO
esperados en tablas service-role); cron de WHOOP/Withings sin un fallo en 3 días; el ajuste diario por recuperación
sigue retirado sin residuos (verificado en `_rwPhase` y `RECOVERY-ONLY`).

## 1 · Veredicto

| Dimensión | 08-sep | **09-sep** | Por qué |
|---|---|---|---|
| Arquitectura | 8 | **8** | Modo manual = mismo pack/prompt/validador. Pero el saneado de la función no está en el camino manual. |
| Coherencia de decisión | 7 | **7** | Dos recuentos del color de recuperación (motor vs prompt); 3 semanas ISO; 5 "lunes"; suelo de pasos en dos sitios. |
| Implementación científica | 6 | **6** | `VOL-CAP` cuenta el box jump como cuádriceps (duro falso cada semana); sin suelo de series; el piloto de kcal no tiene palanca hacia abajo y no lee la composición; LEA inejecutable; "no subir km 2-3 semanas" ausente. |
| Funcionalidad | 5 | **7** | Los P0 del 08 cerrados. Nuevos: aplicar un plan del coach apaga run/walk para siempre; km del coach invisibles; backup sin `meals`/`foods`. |
| Visual / UX | 5 | **7** | Identidad y tokens bien. 3 fallos visibles (cadena ES, toast recortado, labels sticky tapadas), sin estado de error, inputs <16 px, `.wcomp-*` copia de `.bw-stat-*`. |
| Integridad de datos | 7 | **6** | La importación de intervals.icu pisa lo que escribe Withings en `wellness`; marca de agua del pull con el reloj del teléfono; webhook WHOOP sin dedupe si falta `trace_id`. |
| Tests | 8 | **8** | Cobertura amplia pero grep-heavy; `strava.js`, `strava-sync`, `steps-ingest`, `sw.js` sin tests; `APP_SHELL` hard-coded. |
| Rendimiento | 7 | **7** | Stats pinta las 4 pestañas al entrar; Coach view fuera del render pass; 14 labels sticky con `backdrop-filter` + transición de `font-size`. |
| Seguridad | 8 | **6** | **`strava-sync` escribe con service role el `user_id` del cuerpo** (RLS saltado con la anon key pública); el backup compartible lleva `stepsSecret` e `intervalsIcuApiKey`. |

## 2 · P0 — verificados en código hoy (rompen, filtran o mienten al usuario)

| ID | Hallazgo | Evidencia | Arreglo |
|---|---|---|---|
| **S-1** | `strava-sync` confía en `body.user_id` y escribe `runs`/`sessions` con service role; el cliente llama con la **anon key** (pública en GitHub Pages). Cualquiera puede insertar filas en la cuenta de cualquier usuario y usar la función como proxy OAuth de Strava. | `supabase/functions/strava-sync/index.ts:122`; `app/strava.js:67,123` | Patrón de `whoop-sync/index.ts:66-73`: usuario desde `Authorization` con `asUser.auth.getUser()`, **ignorar `body.user_id`**; `strava.js` llama con la sesión (`supa.functions.invoke`, como `integrations.js:171`). Errores de PostgREST/Strava al log, no al cliente (`:126, :208, :262`). |
| **S-2** | `exportBackup()` exporta `settings` entero → `stepsSecret` e `intervalsIcuApiKey` salen por la hoja de compartir de iOS. | `app/app.js:11665`; `:11636`, `:5493` | Lista `BACKUP_REDACT = ['stepsSecret','intervalsIcuApiKey']` aplicada al export; toast lo dice. Test en `verify-home-render`. |
| **D-1** | La importación de wellness desde intervals.icu conserva las claves `whoop*` y las fisiológicas de WHOOP, pero **pisa lo que escribe el servidor desde Withings** (`weightSource`, `weightMeasured`). Comprobado en Supabase: las filas del 7, 8 y 9-sep las han perdido. | `app/whoop.js:352-367` (`smartPut('wellness', compact)`); `_shared/withings-sync.ts` (mirror) | Conservar en `compact` **toda clave de `prev` que intervals.icu no aporte** (no una lista blanca de WHOOP): `for (k of prev) if (!(k in compact)) compact[k] = prev[k]`. Test en `verify-whoop-context` con una fila que traiga `weightSource:'withings'`. Y en `coach-facts.js:_weightDays` etiquetar `wellness.weightMeasured` con `weightSource`, no `'intervals.icu'` fijo. |
| **D-2** | `BACKUP_STORES` no incluye `meals` ni `foods`: el registro de comida que empieza hoy no se respalda; un restore los pierde. | `app/app.js:11116-11122` vs `supabase-sync.js:437` | Añadir los dos; test que exige `BACKUP_STORES ⊇ SYNC_STORES` (menos `sync_queue`). |
| **L-1** | **`VOL-CAP` dispara *hard* sobre el plan vivo**: el box jump (`muscle:'Quads'`, 3 series) + sentadilla 4 + hack 3 + bss 3 + leg extension 3 = 16 > 14. Cada propuesta que no reescriba las piernas llega en rojo; el modo manual se niega a escribir sin `--allow-hard`; en modo API se gasta la regeneración; y el coach ve 16 series cuando son 13. La Home ya lo corrige sólo en pantalla (`renderMuscleVolume`). `hybrid1` tiene `sled-push` como `Quads` (6 series): con el híbrido el cap sube a 22. | `app/app.js:89, 252`; `coach-facts.js:1832-1841` (`_setsPerMuscle`), `:2993-3016` (`_vpSetsPerMuscle`); fixture del test con `'Power'` (`verify-plan-validator.mjs:73`) | En los dos contadores excluir los ids con patrón `plyometric`/`conditioning`/`carry` o en `_MEASURE_EXERCISES` y sumarlos en una fila `Power`; semilla: `box-jump` y `sled-push` → `muscle:'Power'` (+ `PLAN_REV`). **Test con la semilla real**, no con fixture. |
| **L-2** | **Aplicar cualquier plan del coach apaga para siempre el motor de run/walk** (`if (!(activePlan && activePlan.running))`): el esquema del coach siempre devuelve `running{weeklyKmTarget,longRunKm,hardSessions}` y `mergeProposal` lo hereda. Se pierde `3 × (3′ trote / 2′ caminata) · HR ≤143`. Y los `distanceKm`/`hrZone` que el coach escribe en `weekTemplate[dow].cardio` **no se pintan**: sólo `durationMin`. | `app/app.js:8630`, `:8374`, `:8622-8626`; `schema.ts:192-196` | Gate por `Array.isArray(activePlan.running.plan) && length` (o "este `dow` tiene `cardio` del coach"); pintar `cardio.distanceKm` y `cardio.hrZone` en la tarjeta del día y el calendario; aplicar `_coachPlanTargetsAreCurrent()` a `running` como a `_coachCardioMin` (`:8371-8381`, E-4 sin cerrar para la carrera). Test en `verify-running-week`/`verify-cardio-progress`. |
| **V-1** | Única cadena en español que sobrevive: **"Backup completo (JSON)"**, y tres botones de export para dos cosas. `verify-i18n` no la caza ("completo" no está en el diccionario). | `app/index.html:770-773`; `app.js:11658` vs `:11124` | `Full backup (JSON)` / `Workouts only (JSON)` (o fusionar handlers); diccionario de `verify-i18n` + `completo|Guardar|Cerrar|Añadir|Actual|Objetivo|Resumen`. |
| **V-2** | `.toast` con `white-space: nowrap` y sin `max-width`: 6 mensajes (uno de v11.69) se salen de una pantalla de 390 px. | `app/style.css:661` | `white-space: normal; max-width: min(340px, calc(100vw - 48px)); text-align: center; line-height: 1.4`. |
| **V-3** | Las `.section-label` sticky quedan tapadas por `.stats-tabs` sticky (mismo `top: 0`, z-index mayor, opaca) en Stats y Nutrición: el estado `.stuck` nunca se ve donde más labels hay. | `style.css:279-281, 1509-1519`; `app.js:12610` | `#view-stats .view-scroll > .section-label, #view-nutrition … { top: 45px }` (o un `.sticky-head` único con tabs + label). |

## 3 · P1

### 3.1 Lógica de entrenamiento y pack (el coach no puede hacer lo que el procedimiento le pide)

| ID | Hallazgo | Evidencia | Arreglo |
|---|---|---|---|
| **F-4** | El paso 6 del prompt ordena "−200 kcal" pero `KCAL-STEP` (≤150) y `KCAL-FLOOR` (2.700/2.400 = el objetivo actual) lo hacen imposible: el piloto **no tiene palanca hacia abajo** y no lo sabe. | `prompt.ts:295-303`; `coach-facts.js:2222, 2248, 2605-2611`; `nutrition.js:47-48` | Reescribir el paso 6: en el suelo, la palanca es el GASTO (pasos REC-009, minutos fáciles hasta la banda 200-300 de END-009), nunca la ingesta; −200 → −150. |
| **F-5** | REC-002 pilota sólo con el peso aunque `trajectory.weight.scale` ya trae `fatMassKgDelta28d`/`ffmKgDelta28d`. Con peso plano y grasa bajando (recomposición, objetivo #1) recortaría kcal por un artefacto. | `coach-facts.js:737-782`; `prompt.ts:252-258, 295-299` | Segundo veto en el paso 6: "si `fatMassKgDelta28d ≤ −0,5` con `ffmKgDelta28d ≥ −0,3` y `deltaFrom ≥ 21 d`, no se toca"; `RECOMP-HOLD` warn en el validador cuando una decisión baja kcal con esa señal a favor. |
| **F-6** | La mitad reactiva de LOAD-004 ("2 sesiones consecutivas de caída") no la calcula nadie: `deloadHint` mira RPE/calidad/wearable, no rendimiento; `readout.summary.regressed` existe por sesión y no se agrega. | `coach-engine.js:1519-1524`; `app.js:4015`; `coach-engine.js:1172-1185` | `facts.progress.performance = {regressedStreak, sessions:[{date, session, regressed, held, progressed}]}` desde los `readout` de 4 semanas; citarlo en el paso 7. |
| **F-7** | Sin suelo de series por músculo (10-14 es sólo techo). Semana viva: Hamstrings 9, Shoulders 7, cadena posterior partida en 3 etiquetas, Bíceps 0 directo. | `coach-facts.js:2438-2444` | `VOL-FLOOR` warn (<10 en variantes ≥4) sobre familias agregadas (Hamstrings+Posterior+Glutes); publicar `plan.plannedSetsPerMuscle` en el pack. |
| **F-9** | Dos recuentos del color de recuperación: motor `≥2 → red, 1 → yellow`; prompt "Verde 0-1 · Amarillo 2 · Rojo ≥3". El modelo puede escribir "yellow" sobre un pack que dice `red`. | `coach-engine.js:1503-1507`; `prompt.ts:242` | Una tabla: exportar `firedSignals` del motor y que el prompt diga "el color es `readiness.readinessColor`". |
| **F-10** | "No subir volumen de carrera las primeras 2-3 semanas de programa/déficit" (CLAUDE.md) no existe en código. | `coach-engine.js:2113-2123` | Pasar `block.weeksIntoBlock` a `_rwPhase`; con `< 2` en bloque nuevo, `weeklyKmTarget = rampFromKm`. |
| **F-11** | Sueño: se guardan consistencia, score, eficiencia, deuda (`sleepNeedSecs`), SpO2; el pack sólo publica duración. READ-006/LONG-004 piden "duración + consistencia". | `_shared/whoop-wellness.ts:97-206`; `coach-facts.js:1689-1695` | `sleep.consistency7`, `sleep.debtHrs7`, `sleep.score7` (medias + n). |
| **F-12** | El disparador de LEA de REC-008 ("2 de [sueño, libido, ánimo, enfermedad] 2 semanas") es inejecutable: sólo el sueño está en el pack. `fatigue/soreness/stress/mood/motivation` **ya se persisten** desde intervals.icu. | `prompt.ts:298-299`; `whoop.js:322-327` | `readiness.subjective = {fatigue7, soreness7, stress7, mood7, motivation7, n7}` y el contador de LEA sobre eso (o quitar el disparador del prompt). |
| **F-13** | REC-006 (`strong`) declarado sin consumidor "porque no hay campo de hidratación"; `hydration`/`hydrationVolume` sí se persisten. `verify-rule-coverage` sólo mira que exista la nota. | `evidence-to-rules.md:321`; `whoop.js:313-314` | Corregir la nota, publicar hidratación en el pack cuando venga, `consumer: engine`. |
| **F-14** | La cintura (veto del piloto) sólo se guarda dentro de la calculadora Navy de 4 campos obligatorios; `wellness.abdomen` de intervals nunca entra en la serie. | `app.js:6846-6857`; `whoop.js:297` | Guardar cintura sola (merge en la fila del día); volcar `abdomen` a `bodyweight[].waist` si no hay manual. |
| **F-16** | "Regenerate" y "Regenerate with a note" usan `_cWeekKey(today())`; "Close the week" usa `_cTargetWeek`. Un domingo regenera la semana que muere a medianoche y, si ya hay propuesta de la siguiente, ni se ve. | `coach.js:1952, 1957` vs `:1814, 1953` | `_cTargetWeek(today())` en los dos handlers. |
| **F-17** | El push de la semana de carrera a intervals.icu/COROS lee `activePlan.running.plan[]`, que ningún plan del coach tiene → cae al store del cron retirado. | `app.js:6116-6117, 6148` | Construir el push desde `weekTemplate[dow].cardio` (+ el patrón de `suggestRunningWeek` cuando la fuente sea la regla). |
| **F-27** | El `next` del readout post-sesión ignora el objetivo del coach de la semana siguiente (`coachTarget: null`). | `app.js:4010-4013` | Pasar el `coachTarget` del plan activo si `coachTargetIsCurrent(activePlan.weekKey, +7 d)`. |

### 3.2 Código, datos y servidor

| ID | Hallazgo | Evidencia | Arreglo |
|---|---|---|---|
| **C-3** | El camino manual **valida pero no sanea**: faltan `ROUND_KG` 1,25, `MAX_NOTE`, `MAX_DECISIONS`, `MAX_CARDIO_SLOTS`, `MAX_TEMPLATE_CHANGES`, `MAX_REQUESTED_DATA`, `MAX_LASTWEEK_BULLETS`, `MAX_WEEK_SUMMARY`; no valida `cardio[]` ni `weekTemplateChanges[]`. Un `kg: 82.3` manual llega al plan. El test guarda 8 de ~20 constantes. | `scripts/coach-manual-review.mjs:300-370` vs `index.ts:800-1085`; `verify-coach-manual-mode.mjs:100-107` | Extraer `sanitizeOutput` a un módulo puro compartido (`_shared/sanitize.ts` importable por Node como `measures.ts`) o replicar los 9 checks; ampliar el test a la lista completa. |
| **C-4** | Marca de agua del pull = reloj del teléfono; `updated_at` = `now()` de Postgres. Un teléfono adelantado deja filas que nunca se bajan — incluida la `proposed` del modo manual. | `supabase-sync.js:446` vs `:392`; `merge_generic_row` | Marca = máximo `updated_at` visto en la respuesta − 60 s. Test de `pullStore`. |
| **C-5** | `saveActiveWorkout()` (async) colgada de `input`/`change` sin `catch`: un fallo de IDB deja de persistir el borrador del entreno **en silencio**. | `app.js:3699, 3720, 4277` | `.catch` con toast único "Could not save the draft". |
| **C-6** | Flujo de actualización del SW: recarga incondicional por dos caminos (`updatefound` + `controllerchange`), `clients.claim()` recarga en la primera instalación, `reg.installing` sin comprobar null, `reg.update()` cada 5 min puede recargar a mitad de una serie. | `app.js:12159-12176`; `sw.js:101, 110` | Un solo camino; `const w = reg.installing || reg.waiting; if (!w) return;`; con `state.activeSession` abierto → chip "New version — tap to reload" en vez de `reload()`. |
| **C-7** | `CARDIO_TYPE_MAP`/`RUN_MODALITIES` duplicados cliente/función con "keep in sync" manual y cero tests. | `app.js:5545, 5566`; `strava-sync/index.ts:13-24` | Test que extraiga los dos literales y falle si difieren. |
| **C-8** | `intervalsIcuApiKey` y `stepsSecret` siguen en `settings/userSettings` sincronizado (F-17 del 08-sep, parcial). Verificado que **no** llegan al prompt. | `app.js:11624, 11637`; `coach.js:2386`; `supabase-sync.js:459` | Ahora: filtrarlos del `smartPut` de `userSettings` (`LOCAL_ONLY_KEYS`) y guardarlos en `localStorage`; definitivo: A-7 (`integration_tokens` `provider='intervals'`). |
| **C-9** | `whoop-webhook` no deduplica sin `trace_id` (nulos distintos en el índice único): 5 reintentos = 5 syncs del mismo sueño. Withings sí construye clave sintética. | `whoop-webhook/index.ts:80`; migración `:199` | `traceId = payload.trace_id ? … : \`${type}:${externalId}\``. |
| **C-10** | Tres semanas ISO: `_isoWeekKeyFor` y una `isoWeekKey` **local que sombrea el global** (hora local, fórmula 1-ene) vs `coach-engine.js:isoWeekKey` (UTC, 4-ene). Rachas de Stats y pack pueden discrepar. | `app.js:2959, 5044`; `coach-engine.js:191` | Borrar las dos locales; llamar al global con la cadena. |
| **C-11** | Cero timeouts de red: 8 `fetch` en cliente y todos los de `_shared` sin `AbortSignal`. Un proveedor colgado bloquea la cadena de renders o deja el `waitUntil` vivo hasta el límite. | `grep AbortSignal app/*.js` = 0; `_shared/*.ts` | `fetchWithTimeout(url, opts, 12000)` en `_shared/http.ts` y gemelo en `app.js`. |
| **C-12** | Webhooks: el evento se inserta `received` antes de procesar; si la instancia muere queda huérfano y el reintento cae en `ignoreDuplicates`. El GC a 60 días borra, no reencola. | `whoop-webhook:137`; `withings-webhook:98` | Job pg_cron que marque `received` > 30 min como `error` y deje que el cron de sync cubra el rango. |

### 3.3 UX (problemas reales, no pulido)

| ID | Hallazgo | Evidencia | Arreglo |
|---|---|---|---|
| **V-4** | No existe estado de error: 76 `.catch(() => [])`, 51 `catch {}`, 44 `innerHTML = ''`. Fallo de lectura ≡ "sin datos". | `app.js` (no hay `showErrorState`); `coach.js:2083` | `showErrorState(container, msg, retryFn)` junto a `showEmptyState`; cablear en los ~12 renderers de tarjeta. |
| **V-5** | `renderStats()` pinta los 22 renderers de las 4 pestañas al entrar. | `app.js:4978-5033` | Pintar sólo el grupo activo; `switchStatsGroup` pinta el nuevo la primera vez; invalidar en `afterWorkoutSaved()`. |
| **V-6** | Tap targets < 44 px en lo más tocado: `.set-check` 32 px, `.stats-tab` ≈29 px, `.coach-week-open`, `.home-link-mono`; `#todays-detail`/`#queue-ahead` son `<span>` clicables. | `style.css:463, 1521-1526, 2525, 1964`; `index.html:189, 199` | 40×40 / `padding: 12px 4px` / áreas ampliadas; los dos `<span>` → `<button>`. |
| **V-7** | 6 `prompt()` y 10 `confirm()` nativos en PWA instalada; `prompt()` no admite multilínea y es la nota que va al modelo. | `coach.js:1948, 1955, 2209, 2214, 1320`; `app.js:10496, 10524, …` | `promptSheet(title, placeholder, multiline)` sobre el `.plate-sheet` existente; primero los 4 del coach y los 2 de pasos. |
| **V-8** | Favicon/apple-touch-icon = PNG de 1,9 MB; `favicon.svg` (487 B) existe y no se referencia; manifest con un solo icono 1024 `any maskable`. | `index.html:11-12`; `manifest.json`; `sw.js` | `favicon.svg`; `icon-192/512.png` + `maskable-512.png`; `apple-touch-icon` 180. |
| **V-9** | Jank: 14 labels sticky con `backdrop-filter` y `transition: font-size` en Ajustes. | `style.css:277-281` | Quitar la transición; fondo opaco en vez de blur. |
| **V-10** | Stats › "Today" tiene 5 bloques de semana y 3 renderers de recuperación; `steps-card` está en Today **y** en Body. | `index.html:474-498` | **Decisión Julian**: pestañas `Now / Week / Body / Strength` (sólo `data-group` + tandas); fusionar los 3 de recuperación en `readiness-signals`. |
| **V-11** | Tarjeta Withings: 10 tiles planos (6 sin dirección), `· N d` repetido en los 10, `visceralFat` sin unidad; y debajo el Navy produce **otro `bfPct`** bajo la misma etiqueta. | `app.js:6734-6782`; `index.html:527-529` | 4 tiles arriba + `<details>` con los 6; `visceralFat` con `idx` y rango; **Decisión Julian**: Navy como *fallback* (sólo sin báscula) y el formulario queda como "Waist" (F-14). |
| **V-12** | La vista Coach duplica la tarjeta de Home peor: sin `focus`, sin diff, sin guardrails, **sin el estado `requested`**; el botón dice "…and ask for the next" en modo manual. | `coach.js:2115-2124, 1798, 1879-1886` | `renderCoachWeekCard({ into: 'coach-week' })`; etiqueta del botón por `coachReviewMode()`. |
| **V-13** | `renderCoachView` hace 7 secciones en serie fuera del render pass. | `coach.js:2078-2093` | `beginRenderPass/endRenderPass` + `Promise.allSettled`. |
| **V-14** | Inputs < 16 px (iOS hace zoom al enfocar): `.set-input` 15 px, `.input-notes`, `.ew-set`, `.ew-rpe-select` 12 px, formulario Navy 14 px. | `style.css:460, 1610, 489, 1727, 1729`; `app.js:6829` | 16 px; ensanchar columnas del grid antes que bajar el tipo. |
| **V-15** | Formulario Navy: 4 inputs numéricos en una fila de 390 px con CSS inline, labels sin `for`. | `app.js:6829-6841` | Grid 2×2, `class="text-input"`, `for/id`. |

## 4 · P2 / P3 (se ejecutan dentro del incremento indicado)

**Motores y research (inc. 2 y 5):** F-8 variante 5 incumple STR-002 en empuje y tirón (**decisión Julian**: lower/upper/upper) · F-15 finisher Z2 sin modalidad (`z2FinisherModality` en semilla + tarjeta + `MODALITY-ON-LEGS` warn; da consumidor a INT-002/SEL-004) · F-18 deriva de RPE a carga igual en el pack (`lifts[id].atSameLoad`) · F-19 cumplimiento de descansos (`blockTimings`) al pack · F-20 `NUT_CARB_TARGETS` muerto (cablear o borrar) · F-21 proteína por comida (`nutrition.proteinPerMeal`) · F-22 MVPA hecho vs banda 200-300 (`cardio.mvpaMinByWeek`) · F-23 suelo de pasos desde `goals.constraints.stepsFloor` · F-24 `bodyweightKg` del `ctx` inexistente en servidor (borrar o usar para g/kg) · F-25 `caveat` en `coach-rules.js` y en la vista Coach · F-26 calor por temperatura (`tempC` de intervals) y no por mes · F-28 aviso en "Cerrar la semana" si `staleness.runs.daysAgo > 2` · GP_ON_TRACK arranca en 0,34 %/sem frente al 0,5 % declarado (alinear o documentar).

**Código (inc. 4 y 5):** C-13 4 enums del modelo sin validar en `sanitizeOutput` · C-14 `todayStr` UTC en función y script (usar `facts.meta.todayStr`) · C-15 `logStepsManual` con `dbPut` y push sin red de seguridad · C-16 `importBackup` con `dbPut` (nunca sube) · C-17 `APP_SHELL` del test hard-coded (derivar de `index.html`) · C-18 `@supabase/supabase-js@2` sin pin y fuera del shell · C-19 `verify-coach-manual-mode` 100 % regex (ejecutar `requestManualCoachReview` en `vm`) · C-20 `steps-ingest` con `!==` y sin `_shared/http` · C-21 `parse-meal-photo` sin constantes ni `usage` · C-22 `closeEvent`/modo cron/`runForAll` duplicados WHOOP↔Withings (`_shared/events.ts`, `_shared/cron.ts`) · C-23 4 `stableStringify` (exportar el generado) · C-24 5 "lunes" y 5 "sumar días" · C-25 `_whoopLocalDateStr` = `dateStr` · C-26 lista de sesiones de pierna ×3 en `coach.js` · C-27 `safeCallVoid` para 4 fire-and-forget · C-28 toasts que esconden la causa + `catch {}` en `clearFutureScheduleOverrides` · C-29 texto crudo de PostgREST al cliente · C-30 dead code (11 símbolos, ≈115 líneas; `PRICE_CACHE_WRITE_1H` rama muerta que `deno check` marca).

**Visual (inc. 3):** V-16 `.stat-tile` única (`.bw-stat`/`.wcomp-stat`/`.bc-stat`/`.ws-stat` byte a byte) · V-17 `--text3` a `#7c7e85` (3,0:1 → 4,5:1, 158 usos) · V-18 `--fs-2sm: 12px`, `--fs-base: 14px`, `.setting-hint` · V-19 reglas CSS muertas/duplicadas (`.empty-state`, `.chart-empty`, `.ws-label` ×2) · V-20 `aria-label` en modales/`.hi-delete`/`.set-check` (`aria-pressed`) · V-21 `.section-label` dentro de `.card` y tres patrones de *Back* · V-22 opciones "(coming soon)" seleccionables · V-23 tile Readiness con CTA a integraciones y `Strain` → `RPE Load` · V-24 `style.display` → `hidden` · V-25 `:focus-visible` global, texto de `requested` sin interioridades, `'en'` → `'en-US'`.

## 5 · Incrementos y orden

Cada incremento = tests primero → código → bump `CACHE_NAME` + etiqueta + `COACH_APP_VERSION` → push (CI bloquea). Agentes en
paralelo sólo donde los ficheros no se cruzan; integro y hago el bump.

| Paso | Incremento | Contenido | Ficheros | Tests primero |
|---|---|---|---|---|
| 0 | **Informe** | Copiar a `docs/audits/2026-09-09-app-audit.md`; `research-log.md`; `pendientes.md`. | docs | — |
| 1 | **v11.70 · P0 + datos + seguridad** (antes del domingo 13) | S-1, S-2, D-1, D-2, L-1, L-2, V-1, V-2, V-3, C-4, C-5, C-9, F-16, C-3 (saneado compartido), `strava-sync` deploy, `whoop-webhook` deploy, `coach-weekly-review` deploy (generated). | `strava-sync/index.ts`, `strava.js`, `app.js` (backup, seed, run/walk gate, cardio paint, autosave), `whoop.js`, `coach-facts.js` (+ `build-fn-assets`), `coach.js`, `supabase-sync.js`, `whoop-webhook/index.ts`, `_shared/sanitize.ts` (nuevo), `scripts/coach-manual-review.mjs`, `index.html`, `style.css` | `verify-plan-validator` (semilla real, `Power`), `verify-home-render` (redact, `BACKUP_STORES ⊇ SYNC`), `verify-whoop-context` (D-1), `verify-integrations-wiring` (strava auth, trace id), `verify-sync-enqueue` (watermark), `verify-coach-manual-mode` (lista completa), `verify-running-week` (gate), `verify-i18n` (diccionario) |
| 2 | **v11.71 · Motores y pack** | F-4, F-5, F-6, F-7, F-9, F-10, F-11, F-12, F-13, F-14, F-17, F-18, F-19, F-22, F-23, F-24, F-27 (+ F-8 y F-15 si Julian aprueba); prompt pasos 2b/6/7; `coach-weekly-review` deploy. | `coach-facts.js`, `coach-engine.js`, `prompt.ts`, `evidence-to-rules.md`, `app.js` (waist, steps target, readout next, push running), `nutrition.js` | `verify-coach-facts`, `verify-plan-validator` (`VOL-FLOOR`, `RECOMP-HOLD`), `verify-coach-fn-wiring`, `verify-rule-coverage`, `verify-running-week`, `verify-set-target` |
| 3 | **v11.72 · UX** | V-4…V-25 (V-10 y V-11 según decisión). | `style.css`, `index.html`, `app.js` (renderers), `coach.js` (vista Coach), `nutrition.js`, `manifest.json`, `sw.js` (iconos) | `verify-visual-tokens` (stat-tile, text3, tokens fs, sin `style.display`), `verify-home-render` (error state, Stats por grupo, tap targets), `verify-coach-wiring` (card `into`) |
| 4 | **fn v5 + servidor** | C-11, C-12 (+ migración), C-13, C-14, C-20, C-21, C-22, C-23, C-29, C-30 (rama muerta). | `supabase/functions/*`, `_shared/*`, `supabase/migrations/2026091x_events_orphans.sql`, `scripts/build-fn-assets.mjs` | `verify-integrations-wiring`, `verify-coach-fn-wiring`, nuevo `verify-strava-steps-fns.mjs` |
| 5 | **v11.73 · Código y tests** | C-6, C-7, C-8 (local-only keys), C-10, C-15, C-16, C-17, C-18, C-19, C-24…C-28, C-30 (dead code); F-20, F-21, F-25, F-26, F-28. | `app.js`, `whoop.js`, `coach.js`, `nutrition.js`, `sw.js`, `index.html`, tests | `verify-home-render` (APP_SHELL derivado), nuevo `verify-cardio-type-map.mjs`, `verify-coach-manual-mode` (runtime en `vm`), `verify-sync-writes` |
| 6 | **A-7 · intervals.icu y Strava al servidor** | Cierra C-8 definitivamente. Diseño propio. | servidor + `whoop.js`/`strava.js` | — |

Paralelismo: **1 solo y primero** (toca casi todo y es lo que hace falta antes del domingo). Después **2 ‖ 3 ‖ 4**:
2 vive en `coach-facts.js`/`coach-engine.js`/`prompt.ts`, 3 en `style.css`/`index.html`/renderers, 4 en `supabase/functions`;
el único cruce es `app.js` (2: formulario de cintura y readout; 3: renderers) → regiones distintas, se integra con cuidado o
2 antes que 3. **5 después de 3** (comparten `app.js`/`sw.js`). 6 al final.

Colisiones que van juntas: L-1 con F-7 (mismos contadores `_setsPerMuscle`/`_vpSetsPerMuscle`) — L-1 en 1, F-7 en 2 sobre el
contador ya arreglado; L-2 con F-17 (los dos leen `weekTemplate[dow].cardio`); F-14 con V-11/V-15 (el mismo formulario Navy);
V-12 con F-16 (mismos handlers de la tarjeta); C-3 con C-13/C-14 (`sanitize` compartido).

## 6 · Verificación

- **Automática:** suite completa en verde en cada push (36 → ~40 ficheros); CI bloquea el deploy.
- **v11.70 (iPhone + Supabase):** `POST strava-sync` con la anon key y un `user_id` ajeno → 401; el backup exportado no
  contiene `stepsSecret` ni `intervalsIcuApiKey` (grep del fichero); tras una sincronización de intervals.icu, `wellness` del
  día conserva `weightSource:'withings'` (SQL); `validatePlanVersion` sobre el plan v26 real → 0 `VOL-CAP`; aplicar la
  propuesta W37 y abrir el miércoles → la tarjeta del día muestra 30 min Z2 ≤143 y el jueves/sábado el patrón run/walk
  o los km del coach, no "Cardio Z2 · 40 min"; el toast largo se lee entero; en Stats la label pegada se comprime.
- **v11.71:** pack real con `progress.performance`, `sleep.debtHrs7`, `readiness.subjective`, `plan.plannedSetsPerMuscle`;
  `RECOMP-HOLD` y `VOL-FLOOR` en `verify-plan-validator`; una revisión del coach (manual o API) cuyo paso 6 hable de pasos
  y minutos, no de −200 kcal.
- **v11.72:** captura de Home/Stats/Coach sin `prompt()` nativo en el flujo de nota; inputs de serie sin zoom en iOS;
  `--text3` ≥ 4,5:1; una sola familia `.stat-tile`; la vista Coach enseña el estado `requested`.
- **fn v5:** `deno check` limpio; un webhook de WHOOP sin `trace_id` reintentado → 1 fila; evento `received` > 30 min →
  `error` por cron; `parse-meal-photo` devuelve `usage`.
- **v11.73:** un `<script src>` nuevo sin añadir al SW rompe el test; `CARDIO_TYPE_MAP` divergente rompe el test;
  `requestManualCoachReview` ejecutada en `vm` produce la fila esperada.

## 7 · Decisiones de Julian (AskUserQuestion, 2026-09-10)

1. **Alcance: todo P0→P3, en incrementos separados** — v11.70 primero y antes del domingo 13; después 2 ‖ 3 ‖ 4 y 5.
2. **Stats: pestañas `Now / Week / Body / Strength`** (V-10) — carga semanal, resumen, timeline, comparación y coach review
   a `Week`; un solo bloque de recuperación en `Now`; pasos sólo en `Body`. Sólo cambian `data-group` y las tandas de
   `renderStats` (que además pasan a pintar por grupo, V-5).
3. **Navy sólo sin báscula; el formulario pasa a "Waist"** (V-11 + F-14) — guarda la cintura sola con merge en la fila
   del día, vuelca `wellness.abdomen` de intervals.icu cuando no hay medida manual, y `renderBodyCompEstimator` (Navy) sólo
   se pinta si `#withings-comp` está oculto. La tarjeta Withings manda sobre el % de grasa.
4. **Variante 5 días = Lower A · Upper A · Upper B** (F-8) — press y pull a 2×/semana; bisagra vía RDL. Cambio de semilla +
   `PLAN_REV`; la variante activa (6 días) no se toca.

Todo lo demás son arreglos de bugs o consolidaciones sin alternativa de producto y se ejecutan sin preguntar. Lo que
sigue siendo de Julian fuera del código: la protección de contraseñas filtradas en Supabase Dashboard › Auth, y elegir
API o Manual antes de cerrar la semana el domingo 13.
