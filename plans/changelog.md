# Plan Changelog

Newest first. One line per weekly review decision.

- **Sección de analítica: marcadores puntuados contra objetivos de guía (2026-08-20, v11.43)** — Julian pidió *"una sección de analítica"* con correlaciones y rangos, sus mediciones puntuadas 1-5, mejoras y suplementación, y exigió por escrito: *"utiliza solamente papers o websites de research valorados científicamente"*. **Lo que aporta y no existía:** el documento anterior solo tenía el rango que imprime el laboratorio, y ahí está el problema — **un ApoB de 110 cae dentro del `66-133` de IACA y es un 2 de 5** contra los objetivos de la ESC/EAS, porque ese rango es un intervalo *poblacional*, no un umbral de riesgo. Nuevo [`data/processed/2026-08-20_analitica-puntuada.md`](../data/processed/2026-08-20_analitica-puntuada.md): 6 paneles, 344 marcadores, rúbrica, variabilidad biológica, acoplamiento mecánico y suplementación. **La rúbrica, que es la parte de diseño:** hay marcadores donde menos es mejor (ApoB), otros donde más es mejor (vitamina D) y otros con **banda óptima y dos colas malas** (HbA1c, ferritina, TSH); una escala que trate los tres igual está mal, así que se resuelve con **intervalos anidados** — se ensanchan del 5 al 1 y gana el primero que contiene el valor, con lo que las dos colas penalizan simétricamente. Más dos reglas que **quitan** puntajes: *no hay 5 sin objetivo publicado* (por eso vitamina D, TSH, creatinina, ferritina y homocisteína topan en 4 aunque el valor sea impecable) y *no más bandas que cortes publicados + 1*. **Tres hallazgos que el rango del laboratorio tapaba:** (1) el ApoB de arriba; (2) **ferritina 191 está a nueve unidades** del >200 µg/L que la OMS marca como riesgo de sobrecarga de hierro en hombres sanos, invisible dentro de un `30-400`; (3) **el "óptimo" de vitamina D no existe** — el 40-60 ng/mL que se repite en todas partes es de la guía de 2011 de la Endocrine Society y **la vigente (Demay 2024) dice explícitamente que no hay evidencia que defina el nivel óptimo**; que 11,2 sea deficiencia sigue siendo cierto por todas las definiciones. **Contraintuitivo y verificado:** en HbA1c "cuanto más baja, mejor" es **falso** — la banda de menor riesgo en ARIC es 5,0-5,4% y por debajo reaparece exceso de mortalidad; su 5,3% cae justo dentro. **Tres correcciones de cosas que dije mal:** el HDL de 65 no es un "✅ bueno" (el `≥60 elevado` es la convención retirada del NCEP ATP III, y ninguna guía vigente fija diana de HDL hacia arriba); *"no es el del síndrome metabólico"* es un diagnóstico negativo que no me corresponde; y la serie de LDL 149→170→143 **mezcla dos laboratorios y dos ecuaciones** — se reprodujo la aritmética y **ninguna ecuación publicada da el 170 de IACA**, mientras Sampson sobre los números de LabCorp da 142,8 ≈ 143; el cambio de fórmula explica ≤5 de los 27 mg/dL y el resto está en el **colesterol total medido**, que bajó 30. **Frontera clínica:** tres revisiones adversariales encontraron **74 problemas, 16 críticos**, casi todos del mismo tipo — menús de dosificación de vitamina D junto a su valor deficiente, un algoritmo diagnóstico de testosterona, diagnósticos por exclusión, un diferencial de hipercolesterolemia familiar. Corregidos antes de escribir. La suplementación sí lleva dosis (creatina, cafeína, proteína: nutrición deportiva con position stand), **la vitamina D no** (corregir una deficiencia documentada es medicina, `LONG-003`). Con los avisos que casi nadie cita: la creatina añade **1-2 kg de agua intracelular** y su objetivo es la báscula; la cafeína a 3-6 mg/kg son 261-523 mg que por la tarde comprometen el sueño, que es la puerta del día siguiente; y el plateau de 1,62 g/kg de proteína de Morton **se derivó excluyendo sujetos en déficit**. **App:** `app/bloodwork.js` (marcadores, rúbrica, 6 paneles) + vista `view-analytics` en Ajustes → Mis marcadores, con **puntaje y antigüedad como ejes separados** a propósito — un 5 de hace tres años no es un 5 de hoy — aviso de caducidad (`LONG-003` lo pedía y no estaba implementado), sección de nunca-medido, y la lista de qué pedir **copiable**. **Los tests encontraron dos filas que mi propia rúbrica no soportaba:** TSH puntuada 3 con un "óptimo <2,5" que **no sale de ninguna guía vigente** (es de los rangos de medicina funcional que la jerarquía de fuentes excluye por escrito) → 4; y urea puntuada 4 estando **fuera** del intervalo de referencia → 2. Se corrigió el documento, no el test. Y **`tests/` se versiona por primera vez** (`verify-bloodwork` compara fila a fila el documento contra el código; `verify-analytics-render` corre la vista con un DOM mínimo): las verificaciones de las sesiones anteriores vivían en el scratchpad y no sobrevivieron, así que la frase "si divergen, falla" era falsa hasta ahora.

- **Backfill de 15 revisiones semanales W19-W33 (2026-08-19)** — el cron no habia corrido desde el 4 de mayo. Reconstruidas con datos reales de Supabase, no de memoria. **Hallazgos:** (1) la fuerza progreso pese a 10 sesiones en 15 semanas — banca 80→90 kg, sentadilla 90→100, OHP 50→55, con RPE entre 7,0 y 7,4, margen que venia de antes del paron y ya casi agotado; (2) **el peso muerto lleva sin tocarse desde el 3 de mayo**, cero sesiones de lowerB en 15 semanas; (3) **W29 conecta sueno y rendimiento**: readiness 48,3 y sueno 6,04 h (ambos minimos del periodo) es exactamente la semana del unico retroceso de carga (remo 60→50 kg), mientras la sentadilla del dia siguiente aguanto — bajo fatiga cae antes lo accesorio; (4) la **mejor** semana fisiologica (W30, frecuencia en reposo 43,5, la minima del ano) fue de **cero** entrenamientos; (5) el sueno subio ~24 min/noche entre el primer y el ultimo tramo, la mejora mas solida del periodo; (6) **movilidad 0 en 15 semanas** con dos contracturas lumbares detras; (7) carreras: Z2 cumplido 3 de 6 y **las tres ultimas por encima del techo**, pero 19 s/km mas rapido a la misma frecuencia cardiaca que en mayo. CTL se mantuvo entre 1 y 6: la recuperacion mejoro porque la carga fue casi nula, no por fitness. `latest.json` programa **W35** con el peso muerto de vuelta a 90 kg con gate de RPE, sentadilla 102,5, OHP 57,5, dominadas +2,5, remo a 60, banca en 90 y carreras en Z2 estricto (<143).

- **Variante Viaje + pliometría + trineo/SkiErg · objetivo 79-81 kg (2026-08-19, v11.42)** — Julian: *"NO estuve entrenando porque estaba de viaje. Sería bueno tener un full body workout sin elementos de gimnasio"* y *"agregar pliometría + trineo + skierg o lo que consideres que es bueno"*. **La adherencia del 30% tenía explicación benigna y señalaba el arreglo real:** ninguna de las variantes funcionaba sin rack y barra, así que un viaje equivalía a no entrenar — los huecos de 11, 12 y 14 días eran viajes. **Variante VIAJE** (`travelA`/`travelB`, selector 🧳): peso corporal, banda opcional, cero gimnasio. Principio de diseño clave — **cada sesión es completa por sí sola** (pierna + empuje + tirón + core), porque en viaje no sabes si harás una o cuatro y ninguna puede dejar hueco; A y B difieren en énfasis, no en cobertura. La intensidad sale de dificultad y proximidad al fallo, no de carga, con la progresión escrita en cada ejercicio (pies elevados, pausas, tempo) para que no degenere en repeticiones infinitas. **Pliometría en `lowerA`**, al principio y en fresco: pogo hops 2×20 + box jump 3×5 = **55 contactos**, dentro del rango 40-80; sólo en ese día para empezar (el tendón adapta despacio) y bajando del cajón **caminando**, que es donde está la lesión. Era el hueco con mejor evidencia del plan: fuerza + pliometría mejoran la economía de carrera, graduado `strong`, y no había nada. **Híbrido trineo + SkiErg** (`hybrid1`): 6×20 m trineo + 5×250 m SkiErg + 4×40 m farmer carry, como **alternativa del cardio del sábado, no día extra** — es literal en la regla. El trineo es concéntrico puro: sin fase excéntrica no hay daño apreciable, mucho estímulo con pocas agujetas y casi sin interferir con sentadilla ni carrera; probablemente lo mejor para un historial lumbar. Todo de baja skill a propósito. **13 ejercicios nuevos** con su patrón (pike push-up, band row, inverted row, SL-RDL, SL glute bridge, split squat, pogo hops, box jump, broad jump, sled push/drag, SkiErg, farmer carry, bird dog, side plank). Bird dog añadido porque el test de ATH-003 exigía anti-rotación en viaje y faltaba — es de los Big 3 de McGill, que es justo la fuente de esa regla. **`goals.md` reescrito:** el objetivo es la báscula, **79-81 kg**, y queda traducido con honestidad — llegar a 80 kg **no** es el 9% de grasa sino ~10,5-11%, porque parte de los 7,1 kg será masa magra incluso haciéndolo bien; 12-16 semanas a 0,5-0,7%/sem; y lo que decide si se llega atlético o flaco es el estímulo de fuerza, no la dieta. `PLAN_REV`→5.

- **Lista única de pendientes + el backfill vuelve a correr (2026-08-18, v11.40)** — Julian: "esto del tope de 6 qué es? de qué estás hablando? Qué es D5? Estamos trabajando desorganizados". **Las dos quejas eran válidas.** Estuve citando etiquetas internas (D1-D7) y jerga ("el tope de 6") sin traducirlas, con la lista de pendientes enterrada en un informe de 500 líneas, y saltando de tarea en tarea según lo que se rompía. **Creado [`docs/pendientes.md`](../docs/pendientes.md)**: lista única, ordenada, en castellano, con un dueño por línea (Julian / Claude), sin Rule IDs sin explicar. Incluye la traducción del presupuesto de dureza (cada sesión puntúa: recuperación 0 · cardio fácil 0,5 · tren superior 1 · tren inferior 2 · umbral/intervalos 2 · benchmark 3; la semana debería sumar 5-6 y la actual suma 7,5) y el estado medido de los datos. **Etiquetas D1-D7 retiradas** del informe y de `ideal-plan-engine-v1.md`, con tabla de equivalencias para los commits antiguos. Regla de trabajo: un punto a la vez, de arriba abajo, y solo interrumpe algo que esté perdiendo datos. **Arreglo de código:** el backfill de cardio se marcaba "hecho" con un booleano y ya estaba puesto desde v11.36 — es decir, desde **antes** del arreglo de `VirtualSki` (v11.39). Como la ventana de sync normal solo mira de ayer a hoy, todo el histórico de SkiErg habría quedado fuera para siempre. Ahora el marcador está **versionado** (`CARDIO_BACKFILL_REV`, mismo patrón que `PLAN_REV`), así que subirlo fuerza una pasada nueva. Y tanto el resultado del backfill como el diagnóstico de importación pasan de `dbPut` a `smartPut`: **suben a Supabase**, así que se puede verificar en remoto qué se importó y qué se descartó sin pedir capturas — que es justo la información que hizo falta para encontrar el fallo de `VirtualSki`.

- **`VirtualSki` faltaba: las sesiones de SkiErg se descartaban (2026-08-18, v11.39)** — Julian preguntó por qué no hay trineo, ski ni nada híbrido en ningún entrenamiento. Verificando salió un bug urgente: el `CARDIO_TYPE_MAP` de v11.36 se escribió **antes** de conocer la integración Concept2 de intervals.icu, que trae RowErg→`VirtualRow`, SkiErg→**`VirtualSki`** (tipo nuevo) y BikeErg→`VirtualRide`. `VirtualRow` y `VirtualRide` ya estaban por casualidad; **`VirtualSki` no**, así que cada sesión de SkiErg se descartaba en silencio justo después de conectar Concept2. Era el arreglo que planifiqué el 17-ago y que quedó sin hacer al pasar a auditar `fullA`. **Corregido:** `VirtualSki`→`ski`; el lookup ahora **normaliza** la clave (espacios/guiones/case) porque la API devuelve `VirtualSki` y la interfaz muestra "Virtual Ski" — no perder sesiones por un espacio; y `_ICU_TYPE_BY_MODALITY` pasa de `ski: 'Workout'` (genérico y encima en la lista de exclusión de importación) a `ski: 'VirtualSki'`. **Además:** el catálogo tenía 13 workouts y **cero de ski** pese a haber un SkiErg en el gimnasio y ser `ski` una modalidad declarada → añadidos `ski_z2_25` (25 min Z2) y `ski_intervals` (8×1 min Z4). El SkiErg es la mejor opción cuando las piernas están cargadas: tren superior y core, impacto nulo (HYB-005, INT-002). 15 workouts en el catálogo. **Pendiente de decisión:** trineo, carries y pliometría siguen sin existir en ninguna sesión — las familias `hybrid` y `athleticism` de `SESSION_TYPES` no las instancia ninguna variante, y `hybrid-session-taxonomy.md` describe 4 tipos de sesión (A/B/C/D) que no existen. Analizado en la respuesta: la ausencia de circuitos híbridos es **defendible** (HYB-001 permite 0-1/sem en déficit, evidencia `weak_extrapolated`, budget ya en 7,5 sobre 6), pero la de **pliometría no lo es** (END-007 es `strong`: fuerza + plyo mejoran la economía de carrera, y la base aeróbica es una de las dos cualidades declaradas en progresión).

- **Corrección del RDL + cuádriceps en fullB (2026-08-18, v11.38)** — Julian rechazó el `fullA` de v11.37: "muy malo... barbell back squat y barbell bench press y luego barbell row y barbell rdl". **Tenía razón.** El hueco que v11.37 detectó (cero isquios directos en la semana de 3/4 días) era real; la solución no: meter un RDL dejaba **cuatro compuestos de barra seguidos** y ponía una bisagra justo después de 4 series de sentadilla pesada, apilando carga axial sobre un historial de dos contracturas lumbares. Se arregló un hueco creando un riesgo mayor. **Cambios:** `fullA` RDL → **Seated Leg Curl** (mismo estímulo de isquios, cero carga espinal; la extensión de cadera ya la cubre el sumo de `fullB`, lo que faltaba era flexión de rodilla) → baja de 4 a 3 compuestos de barra. `fullB` **+ Leg Extension**: no tenía **nada** de cuádriceps, que dependía entero de la sentadilla de `fullA`, un solo día → ahora 7 series en 2 días (STR-002); v11.37 tampoco lo vio. Volumen de la semana de 3/4 días 25 → 31 series (sigue siendo mantenimiento; el aumento son dos ceros que se rellenan). **Nuevo chequeo automático de carga axial** para que no se repita: las full-body admiten 1 lift de carga espinal pesada y 3 compuestos de barra; los días de pierna dedicados admiten 2 (squat + RDL en `lowerA` es estándar, con 48 h hasta el siguiente día de pierna). Nota sobre el propio test: su primera versión aplicaba el tope de 1 a todo y marcaba `lowerA` como fallo — la regla estaba mal calibrada, no el plan; un test demasiado estricto también miente. **`EQUIP_SUBS` retirado:** existía desde T4 y sus únicos dos usos en todo el código eran su definición y una frase del preview afirmando que "cada día tiene alternativas si falta equipo" — sin ningún mecanismo detrás. La capacidad real es `showSwapUI` (T5.2), que persiste el cambio y es reversible; el preview ahora apunta ahí. **Nota de la variante 4** corregida: añade un día aeróbico, no fuerza (añadir un 3er día de fuerza subiría su budget a 7 sobre un tope de 6 y contradiría el propósito de la variante → sigue en D5). `PLAN_REV` → 4. El plan vivo de 6 días mantiene sus 83 series sin cambios.

- **Auditoría de Full Body A (2026-08-17, v11.37)** — Julian pidió auditar `fullA` con criterio crítico. `fullA`/`fullB` son las **únicas** sesiones de fuerza de las variantes de 3 y 4 días. **Lo que estaba bien:** compuestos, orden, RPE 7-8 (STR-004), descansos (STR-006), 5 de 6 compuestos tocando 3-6 reps (STR-005); y el volumen bajo (25 series) **no era un error** sino dosis de mantenimiento deliberada — ACSM 2026 reconoce dosis mínimas. El problema era la composición. **Hallazgo 1: ATH-003 incumplida Y estructuralmente indetectable.** `fullA` cerraba con Cable Crunch (flexión espinal cargada) y `fullB` con Hanging Leg Raise (flexión de cadera): cero anti-rotación/anti-extensión, con dos contracturas lumbares en el historial, cuando ATH-003 es `strong` y nombra Pallof y ab wheel *por* ese historial. Y `MOVEMENT_PATTERNS` metía todo el core en un bucket `'core'`, así que cualquier chequeo de "core 2×/sem" daba la sesión por buena — **la regla estaba bien escrita y era inaplicable**. Taxonomía partida en `core-anti-rotation`/`core-anti-extension`/`core-flexion` + `isCorePattern()`. **Hallazgo 2: cero series directas de isquios** en toda la semana de 3/4 días; lo único posterior eran 3 series de sumo, que lleva gate lumbar → en un mal día la cadena posterior quedaba en nada. **Cambios:** `fullA` Cable Crunch→Pallof y +RDL 2×8-10@7 (13→15 series, ~44 min, bisagra a 2×/sem); `fullB` Hanging Leg Raise→Ab Wheel. **Y lo que encontró el test, que la auditoría no vio:** al correr el chequeo de ATH-003 sobre las 4 variantes, la de 6 días —la viva— tenía el mismo fallo (solo Pallof en lowerB, resto flexión) → `lowerA` Cable Crunch→Ab Wheel. Core del plan vivo queda 3/3/3, volumen intacto en 83 series. `PLAN_REV`→3. **Documentado sin arreglar:** la variante 3 decía "Viaje / sin gym" y `fullA` exige rack+barra+banco+cables (etiqueta corregida, pero **no existe** variante sin gimnasio); `EQUIP_SUBS` es decorativo (la UI afirma que hay alternativas y no hay mecanismo); la variante 4 no aporta fuerza sobre la de 3 días (acoplado a D5, mejor con carga real). **Tercera vez el mismo patrón:** cola de sync, filtro de actividades y ahora ATH-003 comparten forma — el sistema no podía ver su propio incumplimiento. Escribir el chequeo fue lo que encontró el fallo en la variante viva.

- **Importar TODO el cardio, no solo carreras (2026-08-16, v11.36)** — Tras destrabar la cola (v11.35) el backlog subió (`workouts` 15→22, `plans` 0→9, `wellness`/`steps` al día), pero Julian seguía sin ver su cardio. **Causa: ambos caminos de entrada filtraban `type === 'Run'` y descartaban en silencio todo lo demás.** `app/app.js` y `supabase/functions/strava-sync/index.ts` tiraban bici, remo, ski, elíptica, caminata y natación; `app/whoop.js` importa solo wellness, nunca actividades. Por eso `sessions` llevaba **0 filas** desde su creación, y el hard-day budget, el volumen semanal y la adherencia operaban sobre una imagen parcial de la carga. Lo más incoherente: `CARDIO_LIBRARY` puede mandar 13 workouts de bici/remo/ski al COROS y la app **no podía leer que se hicieran** — catálogo de ida y no de vuelta, en un sistema que declara el cardio "cualidad co-igual". **Arreglos:** `CARDIO_TYPE_MAP` (23 tipos → 8 modalidades) compartido por ambos caminos; carreras siguen en `runs` (distancia/ritmo/GAP) y el resto va a `sessions` con `family: cardio` (o `recovery` para caminatas); **fuerza excluida a propósito** (`WeightTraining`, `Workout`, `Crossfit`, `Yoga`) para no duplicar el log de gym ni inflar el budget; `SESSION_TYPES.cardio.modalities` amplía a elliptical/swim/walk; `dedupeSessions()` casa por (fecha + modalidad + duración ±2 min) prefiriendo intervals.icu; `computeHardDayBudget` pasa a leer sesiones dedupeadas; **backfill one-shot desde 2026-04-01** en ventanas mensuales, idempotente por `icu_{id}`; `drainSyncQueue()` tras cada pull para que lo importado suba en la misma sesión (antes esperaba a la siguiente apertura); y **diagnóstico visible** de qué se importó y qué se descartó por tipo, en consola y en la tarjeta de intervals.icu. **Patrón de fondo, tercera vez:** el descarte silencioso. `drainSyncQueue` hacía `break` sin avisar, el filtro tiraba actividades sin avisar, Settings decía "backed up automatically" pasara lo que pasara. **Pendiente:** la carrera del 16-ago sigue sin aparecer; el diagnóstico nuevo dirá si llegó a intervals.icu y con qué tipo. La Edge Function de Strava requiere `supabase functions deploy` para tener efecto (hoy los 10 runs vienen de intervals.icu, ninguno de Strava).

- **Sync arreglado + D1/D2/D3 (2026-08-16, v11.35)** — Julian reportó haber entrenado 2 veces esa semana y salido a correr, sin nada en Supabase desde el 30-jun. **Causa raíz: mensaje envenenado con head-of-line blocking.** `drainSyncQueue()` hacía `break` ante el primer fallo; el elemento que fallaba era un upsert al store `plans`, cuya **tabla nunca se creó en Supabase**. `applyIdealPlan()` lo encoló el día que salió v11.28 (**2026-06-30**, fecha que coincide exactamente con el último push correcto) y congeló toda la cola durante 7 semanas. Explica también las 0 filas de `sessions`: el logger de Cardio empezó a escribir ahí en v11.29, un día *después* del atasco. **Nada se perdió** — IndexedDB conserva todo y las carreras están además en COROS/Strava/intervals.icu. **Arreglos:** tablas `plans` y `exercises` creadas (migración `add_plans_and_exercises_sync_tables`, patrón de `sessions`); `drainSyncQueue` pasa de `break` a `continue` con contador de intentos y **cuarentena** para fallos permanentes, y colapsa upserts superseded; estado real del sync visible en Settings y aviso en Home (antes decía "Data backed up automatically" incondicionalmente); **backup JSON completo** (el CSV omitía `sessions` y `mobility_sessions`); `navigator.storage.persist()` en `init()`; `mobility_sessions` añadido al pull. **D1:** `isDeloadWeek` se ancla al bloque de 5 semanas del IDEAL vía `deloadAnchorWeek` — llevaba sin dispararse desde mayo porque seguía siendo `wk===5||wk===9` de abril; el ancla se fija a la semana actual, primer deload 4 semanas después. Retirado `getRunsThisWeek`. **D2:** Pec Deck de `upperA` a `upperB` y Lat Pulldown a `upperA` → empuje horizontal y tirón vertical **2×/sem** (STR-002); pecho sigue en 10 series, pero **espalda 11→14 y total 80→83**, un aumento de volumen en déficit que queda explícito. OHP se deja a propósito en 1×/sem. **D3:** la variante de 5 días quita `upperB` en vez de `lowerB` → lower/upper/lower con el sumo intacto; su budget sube a 6,5 (material para D5). Añadido `PLAN_REV` para que una edición de sesiones llegue a un teléfono que ya tenga la misma etiqueta de plan. **Pendiente para Julian:** abrir la app para que drene el backlog; después, backfill de W19-W32.

- **Auditoría del sistema (2026-08-16)** — Auditoría completa del knowledge base a petición del usuario. Informe en [`../assessments/2026-08-16_system-audit.md`](../assessments/2026-08-16_system-audit.md). **El contenido del plan NO se tocó** (decisión del usuario: informar, él decide). Hallazgos: (1) **el deload no se dispara desde mayo** — `isDeloadWeek` sigue anclado al programa de abril y devuelve `false` para siempre; (2) **el espejo de datos en Supabase lleva apagado desde ~2026-06-30**, sin workouts desde el 25-jun, así que no hay backfill posible de las 14 revisiones que faltan (W19-W32) ni forma de refrescar el perfil; (3) **3 de los 6 patrones se entrenan 1×/sem** en la variante 6 del IDEAL, con 10 series de pecho en una sola sesión, contra STR-002 y contra lo que el propio doc afirma; (4) **la variante 5 elimina el peso muerto** al caer `lowerB` en vez de un día de upper; (5) budget planificado 7,5 contra tope 6; (6) el 100% del cardio prescrito es fácil pese a declarar 80/20 y "sesión de calidad"; (7) **GEN-001 reatribuida y degradada a `expert`** — sus fuentes (Huiberts 2024, Soligard 2016) no sostenían el claim y nunca tuvo ficha de verificación. **Corpus:** +11 reglas → 70 total: módulo 11 longevidad (`LONG-001..004`), módulo 12 ambiente/calor (`ENV-001..002`), y `REC-006..009` + `ATH-006` que formalizan acciones ACSM que ya se practicaban sin Rule ID. **Analíticas:** procesadas las 5 de `data/raw/Blood Tests/` → [`../data/processed/2026-08-16_blood-markers.md`](../data/processed/2026-08-16_blood-markers.md); vitamina D 11,2 ng/mL (deficiencia, 2023, nunca re-medida) y LDL 149→170 con ApoB 110 — para el médico, no para el plan; testosterona nunca medida. **App v11.34:** el Z2 finisher (80 min/sem, la tesis del bloque) existía solo como texto — ahora es prescripción visible, registrable y enviable al COROS; y el catálogo de cardio recomienda y avisa **sin bloquear** ningún envío. **Decisiones abiertas D1-D7 al final del informe.**

- W18 review (2026-05-04): **Three lifts progressed cleanly** (Row +5, Squat +1 rep × 4, OHP all 4 sets done at 115). Sumo DL +20 lb landed without back symptoms. Bench at ceiling (RPE 9 set 4 → hold 205). **Wellness signal: over-reaching pattern** — HRV ↓14% vs baseline, RHR +5 sustained, readiness avg 56, sleep 6.3h avg. Pre-programmed W19 deload validated by data. Hip Thrust + Pallof skipped 2 wks running → Hip Thrust to position #1 in Lower B (enforced even on deload), Pallof dropped (replaced by extra Ab Wheel on Lower A). Nutrition logging 3/7 (43%) → blocks nutrition adjustments per <50% rule, fix logging first. Bodyweight measurement reliability collapsed (intervals.icu W18 pulls all forward-fills) — manual weigh-in 4-7×/wk required. W20 post-deload progression: Bench HOLD 205, Squat +5 (220), Row +5 (155), OHP +5 (120), Sumo DL HOLD 245 (wait 2 wks before next +), Chins HOLD +20.
- v10.24 PWA (2026-05-02): Three home/UX improvements. (1) **Week label** — banner now shows "Week N · Day D" (drops broken "of 9", adds program-day count so it's unambiguously training-week, not ISO calendar week); progress bar reset to cycle within current 9-week block; unified `state.settings.programStart`→`startDate` (was a dead duplicate). (2) **Body insights** — added goal-weight setting + ETA banner ("at X kg/wk you reach Y kg in ~N weeks"), no-log-2d nudge, plateau detection (7d-avg unchanged >0.3 kg over 14d → suggest -150 kcal/+1k steps), and weigh-in guidance copy (daily, AM, fasted). (3) **Quick training mode** — toggle pill at top of workout view; when ON, compounds keep full sets, accessories drop a set, isolation/core to 1 set; flag persisted on workout record (`quick:true`); only togglable before any set is checked. Cache → v10.24.
- W19 ACSM refinements (2026-05-02, autorizado por Julian post-feedback): Aplicados 6 cambios al programa para próxima semana (W19 deload) + Block B refinado. (1) **Chest 7→10 sets/sem** — Cable Fly añadido a Upper A (Currier 2026 dose-response 10 sets/muscle/wk). (2) **Eccentric overload 3 s** formal en 4 accesorios fijos: Incline DB Press, RDL, Incline Curl, Leg Curl. (3) **Warm-up con reps explícitas** (bug encontrado por usuario: tabla solo nombraba mobility sin sets/reps; tiempo real ~12 min, no "3-5 min"). (4) **Sumo DL guidance post-contractura W16** — máx +5 lb/sem, RPE-gated; W19 baja a 200 lb × 4 (deload + recovery), corrige el target W18 que era +15 lb a 240 lb. (5) **Hidratación pre-sesión 500 mL** dentro de las 2 h (Thomas 2016: 5-10 mL/kg). (6) **Carb periodization** por tipo de día (Lower 350 g, Upper 285 g, Long-run 310 g, Rest 250 g) sin cambiar calorías totales. (7) **LEA monitoring semanal** (libido, sleep, illness, mood) — EA marginal en días de entreno (27 kcal/kg FFM/d vs threshold ACSM 30). Trigger automático de diet break si 2 markers empeoran ≥2 puntos por 2 semanas. Headers de `training-plan.md` y `nutrition-notes.md` actualizados a v4.0 / v3.0. Reassessment formal en W23.
- W17 (2026-05-01): Sumo DL +15 lb (240×6) — flat RPE 7 across 4 sets last week, clearly under-loaded. Squat +5 (220), Row +5 (150), Bench hold (205, RPE creeping), OHP focus on completing 4th set. Mobility 3× mandatory after W16 back contracture. Lower B reorder: Hip Thrust before BSS. Upper B reorder: lateral/curl before Landmine Row.
- W16 (2026-05-01, backfill): Hold loads, mandatory mobility before any heavy session, skip running until back symptom-free. Reason: Apr 14 Upper A abandoned mid-session due to back contracture — direct consequence of zero mobility under heavy compounds.
- W15 (2026-05-01, backfill): Hold all loads — first-week data, insufficient signal to progress. Make Lower B Friday non-negotiable. Start mobility (1 session min) and protein logging (3 days min). Reason: Lower B missed Friday, no posterior-chain hinge in week 1.
